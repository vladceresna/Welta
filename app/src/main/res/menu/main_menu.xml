<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/actfil"
        android:icon="@drawable/ic_baseline_filter_list_24"
        android:title="Popular filters"
        android:contentDescription="Popular filters"
        app:showAsAction="always" />
    <item
        android:id="@+id/profile"
        android:icon="@drawable/ic_baseline_account_circle_24"
        android:title="Profile"
        android:contentDescription="Profile"
        app:showAsAction="always" />
    <item
        android:id="@+id/checkupd"
        android:title="Check updates"
        android:contentDescription="Check updates"
        app:showAsAction="never" />
    <item
        android:id="@+id/abouti"
        android:title="About"
        android:contentDescription="About"
        app:showAsAction="never" />
    <item
        android:id="@+id/exitacc"
        android:title="Log out"
        android:contentDescription="Log out"
        app:showAsAction="never" />

</menu>